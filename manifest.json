{ "manifest_version": 2,

  "name": "native obj investigator",
  "description": "check native objects for trickery",
  "version": "0.0.1",
  "permissions": [
    "activeTab"
  ],
  "background": {
  	"scripts": ["investigator.js"],
  	"persistent": false
  },
  "browser_action": {    
    "default_icon": "icon.png",
    "default_title": "find native obj trickery"   
  }
}